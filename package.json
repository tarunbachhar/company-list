{
  "name": "company-list",
  "version": "1.0.0",
  "description": "full-stack-company-list",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-install":"npm install --prefix client",
    "start":"node server.js",
    "server":"nodemon server.js",
    "client":"npm start --prefix client",
    "dev":"concurrently \"npm run server\" \"npm run client\"",
    "heroku-postbuild":"NPM_CONFIG_PRDUCTION=false  npm install --prefix client && npm run build --prefix client"
  },
  "author": "Tarun Bachhar",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.3",
    "concurrently": "^3.6.0",
    "express": "^4.16.3",
    "mongoose": "^5.2.3"
  }
}
